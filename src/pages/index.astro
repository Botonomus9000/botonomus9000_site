---
// Main page for BOTONOMUS9000
---

<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>BOTONOMUS9000 - signals from chicago</title>
        <link rel="preconnect" href="https://fonts.googleapis.com" />
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
        <link
            href="https://fonts.googleapis.com/css2?family=Bebas+Neue&family=JetBrains+Mono&display=swap"
            rel="stylesheet"
        />
        <style>
            * {
                margin: 0;
                padding: 0;
                box-sizing: border-box;
            }

            body {
                background-color: #0a0a0a;
                color: #fff;
                font-family: "JetBrains Mono", monospace;
                display: flex;
                flex-direction: column;
                align-items: center;
                justify-content: center;
                min-height: 100vh;
                padding: 20px;
            }

            .container {
                text-align: center;
                max-width: 800px;
            }

            .avatar {
                width: 100%;
                max-width: 400px;
                height: auto;
                margin-bottom: 40px;
                image-rendering: pixelated;
                image-rendering: -moz-crisp-edges;
                image-rendering: crisp-edges;
                transition: all 0.1s ease;
                cursor: pointer;
            }

            .avatar:hover {
                animation: glitch 0.15s infinite;
            }

            @keyframes glitch {
                0% {
                    transform: translate(0) scale(1);
                    filter: hue-rotate(0deg) saturate(1);
                    opacity: 1;
                }
                10% {
                    transform: translate(-8px, 5px) skew(3deg) scale(1.02);
                    filter: hue-rotate(90deg) saturate(3);
                    opacity: 0.8;
                }
                20% {
                    transform: translate(7px, -6px) skew(-4deg) scale(0.98);
                    filter: hue-rotate(-120deg) saturate(2);
                    opacity: 1;
                }
                30% {
                    transform: translate(-6px, 8px) skew(2deg) scale(1.03);
                    filter: hue-rotate(180deg) saturate(4);
                    opacity: 0.7;
                }
                40% {
                    transform: translate(9px, -4px) skew(-5deg) scale(0.97);
                    filter: hue-rotate(-90deg) saturate(2.5);
                    opacity: 1;
                }
                50% {
                    transform: translate(-5px, 7px) skew(4deg) scale(1.01);
                    filter: hue-rotate(270deg) saturate(3.5);
                    opacity: 0.9;
                }
                60% {
                    transform: translate(6px, -8px) skew(-3deg) scale(0.99);
                    filter: hue-rotate(-180deg) saturate(2);
                    opacity: 1;
                }
                70% {
                    transform: translate(-7px, 4px) skew(5deg) scale(1.02);
                    filter: hue-rotate(150deg) saturate(4);
                    opacity: 0.8;
                }
                80% {
                    transform: translate(8px, -5px) skew(-2deg) scale(0.98);
                    filter: hue-rotate(-60deg) saturate(3);
                    opacity: 1;
                }
                90% {
                    transform: translate(-4px, 6px) skew(3deg) scale(1.01);
                    filter: hue-rotate(120deg) saturate(2.5);
                    opacity: 0.9;
                }
                100% {
                    transform: translate(0) scale(1);
                    filter: hue-rotate(0deg) saturate(1);
                    opacity: 1;
                }
            }

            h1 {
                font-family: "Bebas Neue", sans-serif;
                font-size: 4rem;
                color: #b00d63;
                letter-spacing: 15px;
                margin-bottom: 20px;
                font-weight: bold;
                text-shadow: 0 0 20px rgba(176, 13, 99, 0.5);
            }

            .tagline {
                font-family: "JetBrains Mono", monospace;
                font-size: 1.2rem;
                color: #b00d63;
                margin-bottom: 40px;
                letter-spacing: 0.1em;
            }

            nav {
                display: flex;
                gap: 30px;
                justify-content: center;
                flex-wrap: wrap;
            }

            nav a {
                font-family: "JetBrains Mono", monospace;
                color: #d38a31;
                text-decoration: none;
                font-size: 1.1rem;
                letter-spacing: 0.05em;
            }

            @media (max-width: 768px) {
                h1 {
                    font-size: 2rem;
                }

                .tagline {
                    font-size: 1rem;
                }

                nav a {
                    font-size: 1rem;
                }

                nav {
                    gap: 20px;
                }
            }

            @media (max-width: 480px) {
                h1 {
                    font-size: 1.5rem;
                    letter-spacing: 8px;
                }

                .tagline {
                    font-size: 0.9rem;
                }
            }
        </style>
    </head>
    <body>
        <div class="container">
            <img
                src={`${import.meta.env.BASE_URL}/avatar.png`}
                alt="BOTONOMUS9000 pixel art avatar"
                class="avatar"
            />
            <h1>BOTONOMUS9000</h1>
            <p class="tagline">nodes from the underground</p>
            <nav>
                <a href="#home">home</a>
                <a href="#blog">blog</a>
                <a href="https://github.com/yourusername" target="_blank"
                    >github</a
                >
                <a href="https://linkedin.com/in/yourusername" target="_blank"
                    >linkedin</a
                >
            </nav>
        </div>
    </body>
</html>
